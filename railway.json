{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "buildCommand": "bun run build --filter=t3"
  },
  "deploy": {
    "startCommand": "sh -c 'mkdir -p /data/.codex /data/projects && PATH=/app/node_modules/.bin:$PATH node apps/server/dist/bin.mjs serve --port $PORT --host 0.0.0.0'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
